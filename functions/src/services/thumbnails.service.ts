const ThumbnailsService = {
  upload: () => {},
};

export { ThumbnailsService };

// const thumbnail =
//   doc.visibility === `permanent` ? doc.thumbnail ?? null : null;

// thumbnail:
// payload.thumbnail.type === `noop`
//   ? thumbnail
//   : payload.thumbnail.type === `remove`
//   ? null
//   : await ThumbnailsService.upload(),
