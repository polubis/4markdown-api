const enum Visibility {
  Private = `private`,
  Public = `public`,
  Permanent = `permanent`,
}

const enum Plan {
  Free = `free`,
  Pro = `pro`,
  Business = `business`,
}

export { Visibility, Plan };
